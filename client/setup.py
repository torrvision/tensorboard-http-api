from setuptools import setup

setup(
    name='pylapiz',
    description='Lapiz client for python',
    author='bosr',
    url='https://github.com/bosr/lapiz',
    packages=['pylapiz'],
    version='0.5',
    install_requires=[
        "requests"
    ]
)
