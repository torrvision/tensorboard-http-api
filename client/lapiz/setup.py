from setuptools import setup

setup(
    name='lapiz',
    description='Lapiz client for Python',
    author='torrvision',
    url='https://github.com/bosr/lapiz',
    packages=['lapiz'],
    version='0.1',
    install_requires=[
        "requests"
    ]
)
